# Backend (Flask)

- /search: returns mock catalog
- /generate: accepts { seed: {mood, energy} }, returns playlist of 50 in same mood/energy
- CORS enabled for local frontend
- To run: `pip install -r ../requirements.txt && python app.py` (from backend dir)
